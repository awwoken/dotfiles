#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
  get)
    if [ -z "${GH_TOKEN:-}" ]; then
      exit 0
    fi
    printf 'username=x-access-token\n'
    printf 'password=%s\n' "$GH_TOKEN"
    ;;
  store | erase)
    ;;
esac
